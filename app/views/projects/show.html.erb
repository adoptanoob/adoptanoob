<%= @project.name %> <%= link_to "Follow Project", new_project_role_path(@project) %><br />
Created by <%= @project.owner %><br />
<%= @project.body %><br />
<% @users.each do |user| %>
  <%= user.username %><br />
<% end %>
Select the skills you have from the project's skills.<br />
<%= collection_check_boxes(@project, :skill_ids, @project.skills, :id, :skill_content) %>
<%= submit_tag 'Follow' %>